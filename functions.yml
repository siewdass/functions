functions:
  frontend:
    app:
      path: /
      handler: /frontend.js
      packages:
        - components
    home:
      path: /home
      handler: /home.js
      packages:
        - components
  backend:
    api:
      path: /get
      handler: /backend.js
      packages:
        - database