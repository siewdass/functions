riser:
  view:
    about:
      path: /about
      handler: /about.js
      packages:
        - components
        - navigation
        - state
    home:
      path: /
      handler: /home.js
      packages:
        - components
  gateway:
    api:
      path: /get
      handler: /backend.js
      packages:
        - database
      dependencies:
        - /something.js