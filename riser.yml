riser:
  views:
    about:
      path: /
      handler: /about.js
      packages:
        - components
        - navigation
        - state
    home:
      path: /home
      handler: /home.js
      packages:
        - components
  gateways:
    api:
      path: /get
      handler: /backend.js
      packages:
        - database
      dependencies:
        - /something.js