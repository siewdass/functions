riser:
  view:
    about:
      path: /about
      handler: /about.js
      packages:
        - components
        - navigation
        - state
    home:
      path: /
      handler: /home.js
      packages:
        - components
  gateway:
    register:
      path: /register
      handler: /register.js
      packages:
        - database
      dependencies:
        - /something.js